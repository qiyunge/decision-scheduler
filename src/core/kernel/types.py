from __future__ import annotations

from typing import TypeVar
# M = (Theta, State, Observation, Pi, Delta, Action, ,Invariant, Goal, J ) # Model
S = TypeVar('S') # State
A = TypeVar('A') # Action
E = TypeVar('E') # Event    
O = TypeVar('O') # Observation
Q = TypeVar('Q') # Theta
